
eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/ride_db
  data:
    redis:
      host: redis
  kafka:
    producer:
      bootstrap-servers: kafka:29092
    consumer:
      bootstrap-servers: kafka:29092
